/**
 * Staging-only role preview (frontend). Navigation visibility is driven by
 * `navigationByRole` — not hardcoded in the sidebar.
 */
export type { PreviewRole as UserRole } from "@/config/previewRole";
export {
  PREVIEW_ROLE_OPTIONS as ROLE_OPTIONS,
  type PreviewRole,
} from "@/config/previewRole";
