/** Re-export global role / view-as context (sidebar, dashboard, navbar). */
export { RoleProvider, useRole } from "@/context/RoleContext";
export type { UserRole } from "@/context/RoleContext";
export type { PreviewRole } from "@/config/previewRole";
